name: /child/broadcast/list
base_url: http://gossapit.xgimi.com
variables:
  data: {"broadcastScene":"1"}
request:
  url: /child/broadcast/list
  method: POST
  json:
    ${get_encrypt_data($data)}
validate:
  - eq: [status_code, 200]
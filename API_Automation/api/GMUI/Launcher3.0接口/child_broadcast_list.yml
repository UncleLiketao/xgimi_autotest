name: /child/broadcast/list
base_url: ${ENV(Host1)}
variables:
    data: { "broadcastScene": "1" }
request:
    url: /child/broadcast/list
    method: POST
    json: ${get_encrypt_data($data)}
validate:
    - eq: [status_code, 200]

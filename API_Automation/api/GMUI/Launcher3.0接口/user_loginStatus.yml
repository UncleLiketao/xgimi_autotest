name: /user/loginStatus
base_url: ${ENV(Host1)}
variables:
    data: { "key": None }
request:
    url: /user/loginStatus
    method: POST
    json: ${get_encrypt_data($data)}
validate:
    - eq: [status_code, 200]
